# API Flow - 测试案例文档

本文档包含 API Flow 项目的测试案例，用于验证系统的核心功能和异常处理能力。

## 测试环境准备

### 前置条件
1. 项目已成功安装依赖 (`npm install`)
2. 前端服务已启动 (`npm run dev`)
3. 已配置 AI 解析设置（Base URL、API Key、Model）
4. 已创建至少一个项目和接口文件夹
5. 建议启用"智能工作流"模式以体验 Agent 多步决策能力

### 测试数据准备
所有测试文档内容见下方各测试案例。

---

## 测试案例 1：标准 API 文档解析（正常场景）

### 测试目标
验证系统能够正确解析标准格式的 API 文档，准确提取接口名称、URL、请求方法和参数信息。

### 输入数据

**测试文档内容：**

```
5.1.1. 用户登录接口

5.1.1.1. 服务地址
POST /api/v1/auth/login

5.1.1.2. 服务描述
用户使用账号密码进行登录认证，返回访问令牌。

5.1.1.3. 输入参数

| 参数名   | 类型   | 必填 | 说明           |
|---------|--------|------|----------------|
| username | string | 是   | 用户账号       |
| password | string | 是   | 用户密码       |
| captcha  | string | 否   | 图形验证码     |

5.1.1.4. 输出参数

| 参数名    | 类型   | 说明               |
|----------|--------|-------------------|
| code     | int    | 状态码，0表示成功  |
| message  | string | 提示信息           |
| token    | string | 访问令牌           |
| expireAt | int    | 令牌过期时间戳     |

5.1.2. 获取用户信息接口

5.1.2.1. 服务地址
GET /api/v1/user/profile

5.1.2.2. 服务描述
获取当前登录用户的详细信息。

5.1.2.3. 输入参数

| 参数名  | 类型   | 必填 | 说明       |
|--------|--------|------|-----------|
| userId | int    | 是   | 用户ID     |

5.1.2.4. 输出参数

| 参数名   | 类型   | 说明       |
|---------|--------|-----------|
| code    | int    | 状态码     |
| data    | object | 用户信息   |
| data.id | int    | 用户ID     |
| data.username | string | 用户名  |
| data.email    | string | 邮箱    |
| data.phone    | string | 手机号  |
```

### 预期结果

系统应成功提取出 **2 个接口**，包含以下信息：

**接口 1：用户登录接口**
- name: `用户登录接口`
- url: `/api/v1/auth/login`
- method: `POST`
- inputParams: 3个参数
  - `username` (string, 必填)
  - `password` (string, 必填)
  - `captcha` (string, 非必填)
- outputParams: 4个参数
  - `code` (int)
  - `message` (string)
  - `token` (string)
  - `expireAt` (int)

**接口 2：获取用户信息接口**
- name: `获取用户信息接口`
- url: `/api/v1/user/profile`
- method: `GET`
- inputParams: 1个参数
  - `userId` (int, 必填)
- outputParams: 4个参数
  - `code` (int)
  - `data` (object)
  - `data.id` (int)
  - `data.username` (string)
  - `data.email` (string)
  - `data.phone` (string)

### 验证步骤

1. 进入"项目管理"，选择已创建的测试项目
2. 点击"上传客户文档"
3. 选择"粘贴"模式，将上述测试文档内容粘贴到文本框
4. 确保"🤖 AI 智能解析"已勾选（默认）
5. 勾选"⚡ 智能工作流"（推荐，体现 Agent 能力）
6. 点击"开始解析"按钮
7. 观察工作流执行进度（每步实时更新状态，运行中的步骤有跳动动画）：
   - Step 1: 快速识别接口清单 - 显示识别到的接口数量和置信度
   - Step 2: 分批提取参数详情 - 显示"正在提取第 N/M 批..."的实时进度
   - Step 3: 质量验证与增强 - 显示验证、增强和过滤的统计信息
8. 等待解析完成，跳转到工作流结果确认页面，确认后进入"解析结果"页面

### 验证点

- [ ] 成功提取 2 个接口
- [ ] 接口名称正确
- [ ] URL 路径完整准确
- [ ] HTTP 方法识别正确（POST 和 GET）
- [ ] 输入参数数量和类型正确
- [ ] 必填/非必填标识正确
- [ ] 输出参数包含嵌套字段（data.id 等）
- [ ] 参数描述完整
- [ ] **（工作流模式）** Step 1 显示置信度为 high 或 medium
- [ ] **（工作流模式）** Step 2 验证通过 2 个接口
- [ ] **（工作流模式）** 质量评分在 80 分以上

### 实际结果

_运行测试后填写实际结果_

---

## 测试案例 2：格式不规范的文档（异常场景 1）

### 测试目标
验证系统对格式不规范、缺少明确章节结构的文档的解析能力和容错性。

### 输入数据

**测试文档内容：**

```
订单提交

这个接口用来提交订单，需要POST到 /order/submit

参数包括：
- orderId 订单编号（必填）
- items 商品列表（必填，数组类型）
- totalAmount 总金额（number类型）
- remark：备注（可选）

返回的数据：
success - 是否成功（boolean）
orderId - 订单号
payUrl - 支付地址

---

## 查询订单状态

请求方式: GET
接口地址: /order/status

入参:
orderId(string) - 订单ID

出参:
{
  "status": "订单状态",
  "createTime": "创建时间",
  "updateTime": "更新时间"
}
```

### 预期结果

系统应尽可能解析出 **2 个接口**，但可能存在以下情况：

**接口 1：订单提交**
- name: `订单提交`
- url: `/order/submit`
- method: `POST`
- inputParams: 4个参数（可能部分参数类型不准确）
  - `orderId`
  - `items` (array)
  - `totalAmount` (number)
  - `remark`
- outputParams: 3个参数
  - `success` (boolean)
  - `orderId`
  - `payUrl`

**接口 2：查询订单状态**
- name: `查询订单状态`
- url: `/order/status`
- method: `GET`
- inputParams: 1个参数
  - `orderId` (string)
- outputParams: 3个参数
  - `status`
  - `createTime`
  - `updateTime`

### 验证步骤

1. 进入"上传客户文档"页面
2. 选择"粘贴"模式，粘贴上述测试文档内容
3. 确保"AI 智能解析"已勾选
4. 勾选"智能工作流"模式
5. 点击"开始解析"按钮
6. 观察工作流执行过程和 AI 的容错表现

### 验证点

- [ ] 没有抛出解析错误
- [ ] 至少提取出 1-2 个接口
- [ ] AI 能够理解非结构化的描述（"需要POST到"、"请求方式: GET"）
- [ ] 能识别不同格式的参数描述（"- 参数名"、"参数名(类型)"）
- [ ] 对缺失的类型信息有合理的默认值
- [ ] **（工作流模式）** Step 1 可能显示置信度为 medium 或 low
- [ ] **（工作流模式）** Step 2 显示增强次数 > 0（自动补全了缺失字段）
- [ ] **（工作流模式）** 有警告信息，但不影响解析结果

### 预期异常行为

以下情况属于可接受的异常：
- 部分参数的类型识别为默认值 `string`
- `required` 字段可能不准确
- 参数描述可能不完整

### 实际结果

_运行测试后填写实际结果_

---

## 测试案例 3：缺少关键字段的文档（异常场景 2）

### 测试目标
验证系统对缺少关键信息（如接口地址、请求方法）的文档的处理能力。

### 输入数据

**测试文档内容：**

```
## 数据同步接口

这是一个用于同步数据的接口。

参数：
dataId - 数据ID
timestamp - 时间戳
content - 内容

返回：
result - 结果

---

发送通知

输入：title, message, userId
输出：success

---

第三个接口

这个接口什么信息都不全。
```

### 预期结果

这是一个压力测试文档，缺少大量关键信息。系统应：

1. **完成解析流程不崩溃**
2. 可能提取出 **0-2 个接口**
3. 显示明确的错误提示或警告信息

### 验证步骤

1. 进入"上传客户文档"页面
2. 选择"粘贴"模式，粘贴上述测试文档内容
3. 确保"AI 智能解析"和"智能工作流"都已勾选
4. 点击"开始解析"按钮
5. 观察系统如何处理极端不完整的文档

### 验证点

- [ ] 系统不会崩溃或白屏
- [ ] 给出清晰的错误提示（如"未解析出任何接口"或显示置信度为 low）
- [ ] 如果提取出接口，关键字段应有合理的默认值
  - url 为空或推测值
  - method 默认为 GET
- [ ] **（工作流模式）** Step 1 显示识别问题列表（issues）
- [ ] **（工作流模式）** Step 2 显示多条警告信息
- [ ] **（工作流模式）** 质量评分较低（< 60 分）
- [ ] 用户能够看到哪些信息缺失

### 预期异常行为

以下情况属于正常的异常处理：
- 提示"未解析出任何接口，请检查文档内容或切换解析方式"
- 提取出的接口缺少 URL 字段
- AI 解析模式可能给出部分推测结果，但规则引擎完全无法识别

### 错误处理验证

系统应在以下位置显示友好的错误信息：
1. 解析过程中显示进度和状态
2. 解析失败后显示具体原因
3. 提供重新上传或切换解析方式的选项

### 实际结果

_运行测试后填写实际结果_

---

## 测试执行报告模板

### 测试环境信息
- 测试日期：
- 测试人员：
- 浏览器版本：
- AI 模型：
- 前端版本：
- 后端版本：

### 测试结果汇总

| 测试案例 | 状态 | 通过的验证点 | 失败的验证点 | 备注 |
|---------|------|------------|------------|------|
| 案例1：标准文档 |  |  |  |  |
| 案例2：不规范文档 |  |  |  |  |
| 案例3：缺失字段文档 |  |  |  |  |

### 发现的问题

| 问题编号 | 问题描述 | 严重程度 | 测试案例 | 状态 |
|---------|---------|---------|---------|------|
| BUG-001 |  | 高/中/低 | 案例X | 待修复/已修复 |

### 测试结论

- 核心功能是否正常：
- 异常处理是否完善：
- 是否满足交付标准：
- 改进建议：

---

## 附加测试建议

### 性能测试
- 测试超大文档（50+ 接口）的解析速度
- 测试 AI 解析的并发处理能力
- 测试文档分片处理的正确性

### 安全测试
- 测试恶意 JSON 注入
- 测试 XSS 攻击向量
- 测试 API Key 的存储安全性

### 兼容性测试
- 测试不同 AI 模型的解析效果
- 测试不同文档格式（Word、PDF、纯文本）
- 测试不同浏览器的兼容性

### 用户体验测试
- 测试解析进度提示的清晰度
- 测试错误信息的可理解性
- 测试操作流程的顺畅性
